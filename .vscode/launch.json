{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Chrome",
            "type": "chrome",
            "request": "launch",
            "url": "http://localhost:5500", // o la URL de tu archivo
            "webRoot": "${workspaceFolder}",
            "file": "${workspaceFolder}/index.html" // si es un archivo local
        }
    ]
}
